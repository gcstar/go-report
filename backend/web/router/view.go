package router

import (
	"github.com/gin-gonic/gin"
)

func AddViewsController(router *gin.Engine) {
	// router.GET("/index", func(c *gin.Context) {
	// 	c.HTML(http.StatusOK, "index.html", gin.H{"name": "index"})
	// })
}
